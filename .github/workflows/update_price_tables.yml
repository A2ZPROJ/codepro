name: Update Price Tables

on:
  schedule:
    # Dia 1 de cada mês, 08:00 UTC (05:00 BRT)
    - cron: '0 8 1 * *'
  workflow_dispatch:
    inputs:
      only:
        description: 'Rodar apenas uma fonte (sanepar, sinapi)'
        required: false
        default: ''
        type: choice
        options:
          - ''
          - sanepar
          - sinapi
      dry_run:
        description: 'Dry run (não escreve no banco)'
        required: false
        default: false
        type: boolean

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Puppeteer precisa de libs de sistema pra rodar Chromium headless
      - name: Install Chromium deps
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq libnss3 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxrandr2 libgbm1 libpango-1.0-0 libcairo2 libasound2 libxshmfence1 2>/dev/null || true

      - name: Install scraper deps
        working-directory: scripts/scraper
        run: npm install --no-audit --no-fund

      - name: Run scraper
        working-directory: scripts/scraper
        env:
          SUPABASE_URL: https://xszpzsmdpbgaiodeqcpi.supabase.co
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          DEBUG: '1'
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: 'false'
        run: |
          ARGS=""
          if [ -n "${{ github.event.inputs.only }}" ]; then
            ARGS="$ARGS --only=${{ github.event.inputs.only }}"
          fi
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            ARGS="$ARGS --dry-run"
          fi
          echo "Running: node index.js $ARGS"
          node index.js $ARGS
